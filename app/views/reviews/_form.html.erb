<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, label: 'Enter a rating please', error: 'Rating is mandatory, please specify', collection: Review::RATINGS %>
  <%= f.submit "add a review", class: "btn btn-primary" %>

<% end %>
